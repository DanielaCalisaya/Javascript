/* factorial-for.js -> Este archivo debe calcular el factorial de 10 utilizando un solo bucle for */

for (let i = 0; i < 10; i++) { // inicialización | condicion | actualización, lo que debe hacer
    console.log(`El numero es ${i}`); // Imprimo el valor de i segun vaya recorriendo
}